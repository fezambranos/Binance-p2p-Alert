{
  "name": "functions",
  "description": "Cloud Function proxy for the Binance P2P price alert app",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest"
  },
  "dependencies": {
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  },
  "private": true
}
